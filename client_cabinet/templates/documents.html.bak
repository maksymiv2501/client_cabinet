{% extends "base.html" %}

{% from "components/page_hero.html" import page_hero %}

{% block title %}Документи на підпис{% endblock %}

{% block page_header %}
{{ page_hero(
    "Документи на підпис",
    "Договори, додатки та додаткові угоди, які потребують підписання клієнтом"
) }}
{% endblock %} 

{% block content %}

<style>
    .documents-page {
        display: flex;
        flex-direction: column;
        gap: 24px;
    }

    .documents-hero {
        background: #111827;
        color: #fff;
        border-radius: 28px;
        padding: 28px;
        border-left: 7px solid #84bd00;

        display: grid;
        grid-template-columns: 1.35fr 0.8fr;
        gap: 24px;
        align-items: center;
    }

    .documents-hero h2 {
        margin: 0 0 8px;
        font-size: 28px;
    }

    .documents-hero p {
        margin: 0;
        color: rgba(255,255,255,0.68);
        line-height: 1.5;
    }

    .secure-box {
        background: rgba(255,255,255,0.08);
        border-radius: 20px;
        padding: 20px;
    }

    .secure-box span {
        display: block;
        color: rgba(255,255,255,0.62);
        font-size: 12px;
        font-weight: 900;
        margin-bottom: 6px;
        text-transform: uppercase;
    }

    .secure-box strong {
        display: block;
        font-size: 18px;
    }

    .documents-kpi-grid {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 16px;
    }

    .documents-kpi-card {
        background: #fff;
        border-radius: 22px;
        padding: 22px;
        border: 1px solid #e5e7eb;
        border-left: 5px solid #84bd00;
        box-shadow: 0 12px 32px rgba(15,23,42,0.05);
    }

    .documents-kpi-card.warning {
        border-left-color: #f59e0b;
    }

    .documents-kpi-card.danger {
        border-left-color: #ef4444;
    }

    .documents-kpi-label {
        color: #64748b;
        font-size: 13px;
        font-weight: 900;
        margin-bottom: 10px;
    }

    .documents-kpi-value {
        color: #111827;
        font-size: 32px;
        font-weight: 900;
        line-height: 1.1;
    }

    .documents-kpi-note {
        margin-top: 8px;
        color: #64748b;
        font-size: 13px;
        font-weight: 700;
    }

    .documents-toolbar {
        background: #fff;
        border-radius: 24px;
        padding: 20px;
        border: 1px solid #e5e7eb;
        box-shadow: 0 10px 28px rgba(15,23,42,0.04);

        display: flex;
        flex-wrap: wrap;
        gap: 12px;
        align-items: center;
    }

    .documents-toolbar input,
    .documents-toolbar select {
        height: 44px;
        border: 1px solid #d1d5db;
        border-radius: 14px;
        padding: 0 14px;
        background: #fff;
        font-weight: 800;
    }

    .documents-toolbar input {
        min-width: 320px;
        flex: 1;
    }

    .documents-panel {
        background: #fff;
        border-radius: 28px;
        padding: 26px;
        border: 1px solid #e5e7eb;
        box-shadow: 0 14px 35px rgba(15,23,42,0.06);
    }

    .documents-list {
        display: flex;
        flex-direction: column;
        gap: 12px;
        max-height: 640px;
        overflow-y: auto;
        padding-right: 6px;
    }

    .document-card {
        display: grid;
        grid-template-columns: minmax(320px, 1.3fr) minmax(420px, 1fr) 240px;
        gap: 20px;
        align-items: center;

        border: 1px solid #e5e7eb;
        border-left: 5px solid #84bd00;
        border-radius: 20px;
        padding: 16px 18px;
        background: #fff;
        transition: 0.18s;
    }

    .document-card:hover {
        transform: translateY(-2px);
        box-shadow: 0 12px 30px rgba(15,23,42,0.08);
    }

    .document-card.waiting {
        border-left-color: #f59e0b;
    }

    .document-card.urgent {
        border-left-color: #ef4444;
    }

    .document-card.signed {
        border-left-color: #84bd00;
    }

    .document-number {
        font-size: 17px;
        font-weight: 900;
        color: #111827;
        margin-bottom: 4px;
    }

    .document-title {
        font-size: 15px;
        font-weight: 900;
        margin-bottom: 5px;
    }

    .document-note {
        color: #64748b;
        font-size: 12px;
        line-height: 1.35;
    }

    .document-meta {
        display: flex;
        flex-wrap: wrap;
        gap: 9px;
        align-items: center;

        color: #64748b;
        font-size: 13px;
        font-weight: 800;
        line-height: 1.45;
    }

    .meta-dot {
        width: 4px;
        height: 4px;
        border-radius: 50%;
        background: #cbd5e1;
    }

    .document-actions {
        display: flex;
        flex-direction: column;
        align-items: flex-end;
        gap: 8px;
    }

    .document-status {
        font-size: 15px;
        font-weight: 900;
        text-align: right;
        color: #111827;
    }

    .document-status-pill {
        padding: 6px 10px;
        border-radius: 999px;
        font-size: 12px;
        font-weight: 900;
        white-space: nowrap;
    }

    .document-status-pill.waiting {
        background: #fff2cc;
        color: #7a5200;
    }

    .document-status-pill.urgent {
        background: #ffe1e1;
        color: #8a0000;
    }

    .document-status-pill.signed {
        background: #eef8d9;
        color: #3a6d00;
    }

    .document-btn-row {
        display: flex;
        gap: 8px;
        flex-wrap: wrap;
        justify-content: flex-end;
    }

    .document-btn {
        height: 34px;
        padding: 0 12px;
        border-radius: 999px;
        border: 0;
        text-decoration: none;
        background: #111827;
        color: #fff;
        font-size: 12px;
        font-weight: 900;

        display: inline-flex;
        align-items: center;
        justify-content: center;
    }

    .document-btn.green {
        background: #84bd00;
        color: #000;
    }

    @media (max-width: 1200px) {
        .documents-hero,
        .documents-kpi-grid,
        .document-card {
            grid-template-columns: 1fr;
        }

        .document-actions {
            align-items: flex-start;
        }

        .document-btn-row {
            justify-content: flex-start;
        }
    }
</style>

<div class="dashboard-page documents-page">

    <section class="documents-hero">
        <div>
            <h2>Документи, які потребують підписання</h2>
            <p>
                У цьому розділі зібрані договори, додатки та додаткові угоди,
                які потрібно скачати, роздрукувати, підписати та передати постачальнику.
            </p>
        </div>

        <div class="secure-box">
            <span>Захищений доступ</span>
            <strong>PDF-посилання можуть бути тимчасовими та самознищуваними</strong>
        </div>
    </section>

    <section class="documents-kpi-grid">
        <div class="documents-kpi-card">
            <div class="documents-kpi-label">Усього документів</div>
            <div class="documents-kpi-value">{{ data.summary.total }}</div>
            <div class="documents-kpi-note">договори, додатки, угоди</div>
        </div>

        <div class="documents-kpi-card warning">
            <div class="documents-kpi-label">Очікують підпису</div>
            <div class="documents-kpi-value">{{ data.summary.waiting_signature }}</div>
            <div class="documents-kpi-note">потрібна дія клієнта</div>
        </div>

        <div class="documents-kpi-card danger">
            <div class="documents-kpi-label">Термінові</div>
            <div class="documents-kpi-value">{{ data.summary.urgent }}</div>
            <div class="documents-kpi-note">наближається дедлайн</div>
        </div>

        <div class="documents-kpi-card">
            <div class="documents-kpi-label">Підписано</div>
            <div class="documents-kpi-value">{{ data.summary.signed }}</div>
            <div class="documents-kpi-note">оброблені документи</div>
        </div>
    </section>

    <section class="documents-toolbar">
        <input
            id="documentSearch"
            type="text"
            placeholder="Пошук за номером, назвою, договором або типом"
            onkeyup="filterDocuments()"
        >

        <select id="documentStatusFilter" onchange="filterDocuments()">
            <option value="all">Усі статуси</option>
            <option value="waiting">Очікує підпису</option>
            <option value="urgent">Терміново</option>
            <option value="signed">Підписано</option>
        </select>

        <select id="documentTypeFilter" onchange="filterDocuments()">
            <option value="all">Усі типи</option>
            <option value="Договір">Договір</option>
            <option value="Додаток">Додаток</option>
            <option value="Додаткова угода">Додаткова угода</option>
        </select>
    </section>

    <section class="documents-panel">
        <h2>Реєстр документів</h2>
        <p class="muted">
            Перелік документів із PDF-посиланнями для скачування, друку та підписання.
        </p>

        <div class="documents-list">

            {% for doc in data.documents %}
                <article
                    class="document-card {{ doc.status }}"
                    data-status="{{ doc.status }}"
                    data-type="{{ doc.type }}"
                    data-search="{{ doc.number }} {{ doc.title }} {{ doc.contract }} {{ doc.type }} {{ doc.status_text }}"
                >
                    <div>
                        <div class="document-number">{{ doc.number }}</div>
                        <div class="document-title">{{ doc.title }}</div>
                        <div class="document-note">{{ doc.note }}</div>
                    </div>

                    <div class="document-meta">
                        <span>{{ doc.type }}</span>
                        <span class="meta-dot"></span>
                        <span>{{ doc.contract }}</span>
                        <span class="meta-dot"></span>
                        <span>створено {{ doc.created_at }}</span>
                        <span class="meta-dot"></span>
                        <span>підписати до {{ doc.sign_until }}</span>
                    </div>

<div class="document-actions">
    <div class="document-status-group">
        <div class="document-status">{{ doc.status_text }}</div>

        <span class="document-status-pill {{ doc.status }}">
            {% if doc.status == "urgent" %}
                Наближається дедлайн
            {% elif doc.status == "waiting" %}
                Потрібна дія
            {% else %}
                Завершено
            {% endif %}
        </span>
    </div>

    <button
        type="button"
        class="document-btn"
        onclick="openPdfPreview('{{ doc.pdf_url }}')"
    >
        PDF
    </button>

    {% if doc.status != "signed" %}
        <label class="document-upload-btn" id="upload-btn-{{ doc.number }}">
            Завантажити підписаний
            <input
                type="file"
                accept=".pdf,.jpg,.jpeg,.png"
                hidden
                onchange="showSignedUpload(this, '{{ doc.number }}')"
            >
        </label>

        <div class="signed-upload-note" id="upload-note-{{ doc.number }}"></div>
    {% endif %}
</div>
                </article>
            {% endfor %}

        </div>
    </section>

</div>

<script>
let currentPdfUrl = "";

function openPdfPreview(url) {
    document.getElementById("pdfPreview").classList.add("active");
    document.getElementById("pdfPreviewFrame").src = url;
    document.getElementById("pdfDownloadBtn").href = url;

    document.body.style.overflow = "hidden";
}

function closePdfPreview() {
    document.getElementById("pdfPreview").classList.remove("active");
    document.getElementById("pdfPreviewFrame").src = "";

    document.body.style.overflow = "";
}

function printPreviewPdf() {
    const iframe = document.getElementById("pdfPreviewFrame");

    if (iframe && iframe.contentWindow) {
        iframe.contentWindow.focus();
        iframe.contentWindow.print();
    }
}

function showSignedUpload(input, documentNumber) {
    const note = document.getElementById("upload-note-" + documentNumber);
    const button = document.getElementById("upload-btn-" + documentNumber);

    if (!input.files || !input.files.length) {
        note.textContent = "";
        return;
    }

    note.textContent = "Обрано: " + input.files[0].name;

    if (button) {
        button.classList.add("uploaded");
        button.firstChild.nodeValue = "Файл завантажено";
    }
}

function filterDocuments() {
    const search = document.getElementById("documentSearch").value.toLowerCase();
    const status = document.getElementById("documentStatusFilter").value;
    const type = document.getElementById("documentTypeFilter").value;

    document.querySelectorAll(".document-card").forEach(function(card) {
        const matchesSearch = card.dataset.search.toLowerCase().includes(search);
        const matchesStatus = status === "all" || card.dataset.status === status;
        const matchesType = type === "all" || card.dataset.type === type;

        card.style.display =
            matchesSearch && matchesStatus && matchesType
                ? "grid"
                : "none";
    });
}

document.addEventListener("keydown", function(event) {
    if (event.key === "Escape") {
        closePdfPreview();
    }
});
</script>
<div class="pdf-preview-overlay" id="pdfPreview">

    <div class="pdf-preview-modal">

        <div class="pdf-preview-header">

            <div class="pdf-preview-title">
                Перегляд документа
            </div>

            <div class="pdf-preview-actions">

                <a
                    id="pdfDownloadBtn"
                    class="pdf-preview-btn"
                    href="#"
                    target="_blank"
                    download
                >
                    Завантажити
                </a>

                <button
                    type="button"
                    class="pdf-preview-btn"
                    onclick="printPreviewPdf()"
                >
                    Друк
                </button>

                <button
                    type="button"
                    class="pdf-preview-close"
                    onclick="closePdfPreview()"
                >
                    ×
                </button>

            </div>

        </div>

        <iframe
            id="pdfPreviewFrame"
            class="pdf-preview-frame"
        ></iframe>

    </div>

</div>


{% endblock %}